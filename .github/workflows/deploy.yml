name: Deploy MultiK8s
on:
  push:
    branches:
      - test

env:
  SHA: $(git rev-parse HEAD)

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Test
        run: |-
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker build -t rallycoding/react-test -f ./client/Dockerfile.dev ./client
          docker run -e CI=true rallycoding/react-test npm test

      - name: Build
        run: |-
          docker build -t rallycoding/multi-client:latest -t rallycoding/multi-client:${{ env.SHA }} -f ./client/Dockerfile ./client
          docker build -t rallycoding/multi-server:latest -t rallycoding/multi-server:${{ env.SHA }} -f ./server/Dockerfile ./server
          docker build -t rallycoding/multi-worker:latest -t rallycoding/multi-worker:${{ env.SHA }} -f ./worker/Dockerfile ./worker

      - name: Push
        run: |-
          docker push rallycoding/multi-client:latest
          docker push rallycoding/multi-server:latest
          docker push rallycoding/multi-worker:latest

          docker push rallycoding/multi-client:${{ env.SHA }}
          docker push rallycoding/multi-server:${{ env.SHA }}
          docker push rallycoding/multi-worker:${{ env.SHA }}

      - name: Apply
        run: |-
          kubectl apply -f k8s
          kubectl set image deployments/server-deployment server=rallycoding/multi-server:${{ env.SHA }}
          kubectl set image deployments/client-deployment client=rallycoding/multi-client:${{ env.SHA }}
          kubectl set image deployments/worker-deployment worker=rallycoding/multi-worker:${{ env.SHA }}
